interface:
  display_name: "Experiment Run Docs"
  short_description: "Record dated experiments in Research-Vault"
  default_prompt: "Use $experiment-run-docs after running or analyzing experiments to document command, config, data, model/checkpoints, results, artifacts, and logs in Research-Vault; update repo docs only for stable project documentation."
