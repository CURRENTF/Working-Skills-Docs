interface:
  display_name: "Experiment Run Docs"
  short_description: "Record dated experiment runs in LeafWiki"
  default_prompt: "Use $experiment-run-docs after running or analyzing experiments to document command, config, data, model/checkpoints, results, artifacts, and logs in LeafWiki by default; update repo docs only for stable runbooks, architecture, parameter semantics, or explicit user-requested fallback notes."
